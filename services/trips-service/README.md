# Trips Service

Отвечает за маршруты и этапы маршрутов.

## Запуск (локально)
1. Установить зависимости (Poetry).
2. Создать `.env` рядом с `pyproject.toml`.
3. Запустить `uvicorn app.main:app --app-dir services/trips-service --reload`.
